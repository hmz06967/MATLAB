dizi=[-4 7 -3 5 -6 -12];
ptop=0;
ntop=0;
a=leng
for i=1:a
    b= dizi(i);
    if b>0
        ptop = ptop+b;
    else
        ntop = ntop+b;
    end;
    oran = ptop/ntop;
end
fprintf('Pozitif toplam:%d',ptop);
fprintf('Negatif toplam:%d',ntop);
fprintf('Oran:%d',oran);